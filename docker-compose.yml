services:
  mongodb:
    image: mongo:7
    container_name: dpm_mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - dpm_mongo_data:/data/db

volumes:
  dpm_mongo_data:
